
---user---
The validator rejected this design (attempt {{attempt}} of {{maxAttempts}}). Fix every error below; fix warnings where you can without creating new errors. Keep everything that is not affected: same style, palette and items where possible. Move or resize items rather than removing them, unless there is no room.

Errors and warnings (JSON, each with a concrete hint):
{{issues}}

Call submit_design again with the complete corrected design.
